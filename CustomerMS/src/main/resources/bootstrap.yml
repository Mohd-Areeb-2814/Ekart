server:
  port: 3336
  
spring:
  application:
    name: CustomerMS
  cloud:
    consul:
      discovery:
        hostname: localhost
      config:
        enabled: true
        prefixes: config
        default-context: applications
        profile-separator: "::"
        format: YAML