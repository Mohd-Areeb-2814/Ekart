server:
  port: 3335
  
spring:
  application:
    name: CartMS
  cloud:
    consul:
      discovery:
        hostname: localhost
      config:
        enabled: true
        prefixes: config
        default-context: applications
        profile-separator: "::"
        format: YAML